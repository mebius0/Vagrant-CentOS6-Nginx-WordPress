server {
	listen		80;
	server_name  	local.dev.plugins;
	charset		utf-8;

	include global/restrictions.conf;
	include global/wp-proxy.conf;
}

server {
	listen			8000;
	server_name		local.dev.plugins;
	charset			utf-8;
	root			/var/www/plugins;
	set_real_ip_from	127.0.0.1;
        real_ip_header		X-Real-IP;

	include	global/restrictions.conf;
	include	global/wordpress.conf;
}
